# Codropshipping 3.0 完整版（Vue 3）上线验收清单

## 代码与安全

- [ ] 根目录 `VERSION` 与四个服务的 `package.json` 均为 `3.0.0`。
- [ ] 发布包名称为 `Codropshipping-3.0-Full-*`，SHA-256 校验通过。
- [ ] CI 的测试、构建、release lint、统一 UI 镜像、Compose 校验和 Secret 扫描全部通过。
- [ ] 生产运行依赖无 Critical/High；构建期剩余风险已记录并接受。
- [ ] `.env`、SQLite/WAL、API Key、Cookie、Shopify 密码未进入提交。
- [ ] 已轮换所有曾通过聊天、截图或临时文件传递的凭证。

## 配置与数据

- [ ] 生产域名、HTTPS、允许来源和上游 Host 正确。
- [ ] 搜索数据库账号为最小权限，管理令牌为独立随机值。
- [ ] 刊登数据库使用命名卷；备份、校验、异机复制和恢复演练完成。
- [ ] 日志未包含顾客隐私数据或认证凭证。

## 客户全链路

- [ ] 注册/登录后进入新客户工作台，不再出现旧后台 UI。
- [ ] `/` 与 `/admin/*` 的响应来自同一个 `ui` 容器和同一发布标签。
- [ ] 全局语言切换能翻译导航、页面字段与提示；默认英语。
- [ ] CoD 商品详情以 `List to my store` 为主操作，默认进入 AI 辅助流程，同时保留 `Manual listing` 并生成可恢复草稿。
- [ ] 标题、描述、SEO 可分段 AI 优化、重新生成、采用与手工编辑。
- [ ] 图片自动生成首版，并可在至少四种风格间生成、选择、上传和删除。
- [ ] 店铺、发货仓、变体、售价和库存校验完整，错误提示可理解。
- [ ] 发布只发送客户最终审核版本；重复点击不会重复创建商品。
- [ ] Shopify 后台可看到正确商品、图片、变体、SEO、库存和状态。
- [ ] 订单、履约、库存、财务和数据洞察页面为统一新 UI，并有真实空状态/错误状态。

## 上线与回滚

- [ ] 发布镜像使用不可变唯一标签，保留上一稳定标签。
- [ ] 上线后 smoke test 和低风险真实订单闭环通过。
- [ ] 监控、告警负责人和应急联系方式已确认。
- [ ] 已验证无需删除数据卷即可回滚应用版本。
